name : learning
on: [push]
jobs:
    run-shell-commands:
        runs-on: ubuntu-latest
        steps:
            
            - name: Copy the code
              uses: actions/checkout@v3

            - name: echo a string
              run: |
                pwd
                ls -la
              



            
